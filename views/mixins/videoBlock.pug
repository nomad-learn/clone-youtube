mixin videoBlock(video={})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=`${video.videoFile}#t=5`, controls=false)
            h4.videoBlock__title= video.title
            h6.videoBlock__view= video.view