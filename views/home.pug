extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in Videos
            +videoBlock({
                title: item.title,
                view: item.view,
                videoFile: item.videoFile
            })